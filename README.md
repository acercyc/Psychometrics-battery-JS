# Psychometrics battery JS
 
## Glass pattern
Use `genGlassPatternXY` to generate xy coodinates 

## Radial frequency
Use `rf_picCreator` to create RF figure data.\
It returns n x n contract values that can use to create a image
